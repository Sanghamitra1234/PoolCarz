import { RideFiltersPipe } from './ride-filters.pipe';

describe('RideFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new RideFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
